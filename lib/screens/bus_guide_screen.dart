import 'package:flutter/material.dart';
import 'bus_guide_main_page.dart';

class BusGuideScreen extends StatelessWidget {
  const BusGuideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const MainPage();
  }
}